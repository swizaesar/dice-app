import styled from "styled-components";

const Style = styled.div`
    padding: 0 50px;
    .point {
        display: flex;
        &-value {
            margin-left: 5px;
        }
    }
`;

export default Style;
